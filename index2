<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <!-- SEO Meta Tags -->
  <title>Atharv Jadhav | Athlete & Business Professional</title>
  <meta name="description" content="Atharv Jadhav - National level athlete with 20+ medals and 99.40 CET percentile. IIM Bangalore student combining athletic discipline with business excellence.">
  <meta name="keywords" content="Atharv Jadhav, IIM Bangalore, National Athlete, Cricket, Football, Basketball, Swimming">
  <meta name="author" content="Atharv Jadhav">
  
  <!-- Open Graph for social sharing -->
  <meta property="og:title" content="Atharv Jadhav | Athlete & Business Professional">
  <meta property="og:description" content="National level athlete with 20+ medals and 99.40 CET percentile">
  <meta property="og:type" content="website">
  
  <!-- Preconnect for performance -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link rel="preconnect" href="https://cdnjs.cloudflare.com">
  
  <!-- Font Awesome Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  
  <!-- Google Fonts -->
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap">
  
  <style>
    /* ===== CSS RESET & VARIABLES ===== */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      /* Sophisticated color palette */
      --primary: #9A8C7C;      /* Warm taupe - primary brand */
      --primary-dark: #7D6F5F;  /* Darker taupe */
      --primary-light: #C4B5A5;  /* Light taupe */
      --secondary: #2C3E50;     /* Deep navy - contrast */
      --accent: #B87D4B;        /* Warm amber - accent */
      --light: #F9F7F5;         /* Off-white background */
      --light-alt: #F5F0EA;      /* Slightly warmer background */
      --dark: #000000;          /* Pure black */
      --text: #1A1A1A;          /* Almost black for text */
      --text-light: #4A4A4A;    /* Dark gray for secondary text */
      --shadow-sm: 0 4px 12px rgba(0, 0, 0, 0.03);
      --shadow-md: 0 8px 24px rgba(0, 0, 0, 0.05);
      --shadow-lg: 0 16px 32px rgba(0, 0, 0, 0.08);
      --border-light: 1px solid rgba(0, 0, 0, 0.08);
      --transition: all 0.3s ease;
    }

    /* Dark mode variables */
    body.dark-mode {
      --light: #1a1a1a;
      --light-alt: #2d2d2d;
      --dark: #ffffff;
      --text: #f0f0f0;
      --text-light: #c0c0c0;
      --border-light: 1px solid rgba(255, 255, 255, 0.08);
      background: #1a1a1a;
    }

    /* ===== TYPOGRAPHY ===== */
    body {
      background: var(--light);
      color: var(--text);
      font-family: 'Inter', sans-serif;
      font-weight: 300;
      line-height: 1.7;
      font-size: 16px;
      transition: background-color 0.3s, color 0.3s;
    }

    h1, h2, h3, h4, h5 {
      font-family: 'Playfair Display', serif;
      font-weight: 500;
      letter-spacing: -0.02em;
      color: var(--dark);
    }

    h1 {
      font-size: 3.5rem;
      margin-bottom: 0.5rem;
      font-weight: 600;
      letter-spacing: -0.03em;
      color: #000000;
    }

    .dark-mode h1 {
      color: #ffffff;
    }

    h2 {
      font-size: 2.2rem;
      margin-bottom: 1.5rem;
      position: relative;
    }

    /* ===== LOADER ===== */
    .loader-wrapper {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: var(--light);
      display: flex;
      justify-content: center;
      align-items: center;
      z-index: 9999;
      transition: opacity 0.5s;
    }

    .loader-wrapper.fade-out {
      opacity: 0;
      pointer-events: none;
    }

    .loader {
      width: 50px;
      height: 50px;
      border: 3px solid var(--primary-light);
      border-top-color: var(--accent);
      border-radius: 50%;
      animation: spin 1s infinite linear;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    /* ===== NAVIGATION BAR ===== */
    .navbar {
      position: sticky;
      top: 0;
      z-index: 1000;
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border-bottom: var(--border-light);
      padding: 0.8rem 0;
      transition: background-color 0.3s;
    }

    .dark-mode .navbar {
      background: rgba(26, 26, 26, 0.9);
    }

    .nav-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-family: 'Playfair Display', serif;
      font-size: 1.5rem;
      font-weight: 600;
      color: #000000;
      text-decoration: none;
      letter-spacing: -0.02em;
    }

    .dark-mode .logo {
      color: #ffffff;
    }

    .logo span {
      color: var(--accent);
    }

    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }

    .nav-links a {
      text-decoration: none;
      color: var(--text);
      font-weight: 400;
      font-size: 0.95rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      transition: var(--transition);
      position: relative;
      padding: 0.5rem 0;
    }

    .nav-links a::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 0;
      height: 2px;
      background: var(--accent);
      transition: var(--transition);
    }

    .nav-links a:hover {
      color: var(--dark);
    }

    .nav-links a:hover::after {
      width: 100%;
    }

    /* ===== THEME TOGGLE ===== */
    .theme-toggle {
      position: fixed;
      bottom: 2rem;
      right: 2rem;
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background: var(--secondary);
      color: white;
      border: none;
      cursor: pointer;
      z-index: 1000;
      box-shadow: var(--shadow-md);
      transition: var(--transition);
    }

    .theme-toggle:hover {
      transform: scale(1.1);
    }

    /* ===== MAIN CONTAINER ===== */
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 2rem;
    }

    /* ===== HEADER SECTION ===== */
    .header {
      padding: 3rem 0 2rem;
      text-align: center;
      border-bottom: var(--border-light);
      margin-bottom: 3rem;
    }

    .header h1 {
      margin-bottom: 0.5rem;
    }

    .motto {
      font-size: 1rem;
      letter-spacing: 4px;
      text-transform: uppercase;
      color: var(--primary-dark);
      position: relative;
      display: inline-block;
    }

    .motto::before,
    .motto::after {
      content: '';
      position: absolute;
      top: 50%;
      width: 30px;
      height: 1px;
      background: var(--primary-light);
    }

    .motto::before {
      left: -40px;
    }

    .motto::after {
      right: -40px;
    }

    /* ===== HERO SECTION ===== */
    .hero-section {
      text-align: center;
      padding: 3rem 2rem;
      background: linear-gradient(135deg, rgba(154,140,124,0.05) 0%, rgba(44,62,80,0.05) 100%);
      border-radius: 30px;
      margin: 2rem 0 3rem;
    }

    .hero-section h2 {
      font-size: 2.2rem;
      margin-bottom: 1rem;
      color: var(--dark);
    }

    .hero-subtitle {
      font-size: 1.2rem;
      color: var(--text-light);
      margin-bottom: 2rem;
    }

    .hero-cta {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    .btn {
      display: inline-block;
      padding: 0.8rem 2rem;
      border-radius: 40px;
      text-decoration: none;
      font-weight: 500;
      transition: var(--transition);
      border: none;
      cursor: pointer;
    }

    .btn.primary {
      background: var(--secondary);
      color: white;
    }

    .btn.secondary {
      background: transparent;
      border: 2px solid var(--primary);
      color: var(--dark);
    }

    .dark-mode .btn.secondary {
      color: white;
    }

    .btn:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }

    /* ===== STATS GRID ===== */
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 1.5rem;
      margin: 3rem 0;
    }

    .stat-card {
      background: white;
      padding: 1.5rem;
      border-radius: 12px;
      text-align: center;
      box-shadow: var(--shadow-sm);
      transition: var(--transition);
    }

    .dark-mode .stat-card {
      background: var(--light-alt);
    }

    .stat-card:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow-lg);
    }

    .stat-number {
      font-family: 'Playfair Display', serif;
      font-size: 2.5rem;
      font-weight: 700;
      color: var(--accent);
      line-height: 1.2;
    }

    .stat-label {
      font-size: 0.9rem;
      color: var(--text-light);
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    /* ===== SECTION STYLING ===== */
    .section {
      margin-bottom: 4rem;
    }

    .section-title {
      font-size: 2rem;
      position: relative;
      display: inline-block;
      margin-bottom: 2rem;
    }

    .section-title::after {
      content: '';
      position: absolute;
      bottom: -8px;
      left: 0;
      width: 60%;
      height: 3px;
      background: linear-gradient(90deg, var(--primary) 0%, transparent 100%);
    }

    /* ===== CARD COMPONENTS ===== */
    .card {
      background: white;
      border-radius: 16px;
      padding: 2rem;
      box-shadow: var(--shadow-sm);
      border: var(--border-light);
      transition: var(--transition);
    }

    .dark-mode .card {
      background: var(--light-alt);
    }

    .card:hover {
      box-shadow: var(--shadow-md);
      transform: translateY(-2px);
    }

    #profile-card {
      border-left: 4px solid var(--accent);
    }

    .profile-text {
      font-size: 1.1rem;
      line-height: 1.9;
      color: var(--text);
    }

    /* ===== TABS SYSTEM ===== */
    .tabs-wrapper {
      margin-bottom: 2rem;
    }

    .tab-radio {
      display: none;
    }

    .tabs-container {
      background: white;
      border-radius: 16px;
      box-shadow: var(--shadow-sm);
      overflow: hidden;
    }

    .dark-mode .tabs-container {
      background: var(--light-alt);
    }

    .tabs-header {
      display: flex;
      background: var(--light-alt);
      border-bottom: var(--border-light);
      padding: 0.25rem;
      gap: 0.25rem;
    }

    .tab-label {
      flex: 1;
      padding: 1rem;
      background: transparent;
      border: none;
      border-radius: 12px;
      cursor: pointer;
      font-family: 'Inter', sans-serif;
      font-weight: 500;
      font-size: 0.9rem;
      letter-spacing: 0.5px;
      color: var(--text-light);
      transition: var(--transition);
      text-align: center;
      display: block;
    }

    .tab-label:hover {
      background: rgba(154, 140, 124, 0.1);
      color: var(--dark);
    }

    .tab-content {
      display: none;
      padding: 2rem;
    }

    /* Radio button functionality */
    #academic:checked ~ .tabs-container #academic-content,
    #sports:checked ~ .tabs-container #sports-content {
      display: block;
    }

    #leadership:checked ~ .tabs-container #leadership-content,
    #languages:checked ~ .tabs-container #languages-content {
      display: block;
    }

    #differentiators:checked ~ .tabs-container #differentiators-content,
    #interests:checked ~ .tabs-container #interests-content {
      display: block;
    }

    /* Active tab styling */
    #academic:checked ~ .tabs-container label[for="academic"],
    #sports:checked ~ .tabs-container label[for="sports"],
    #leadership:checked ~ .tabs-container label[for="leadership"],
    #languages:checked ~ .tabs-container label[for="languages"],
    #differentiators:checked ~ .tabs-container label[for="differentiators"],
    #interests:checked ~ .tabs-container label[for="interests"] {
      background: white;
      color: var(--dark);
      box-shadow: var(--shadow-sm);
      font-weight: 600;
    }

    /* ===== LIST ITEMS ===== */
    .item {
      margin-bottom: 1.8rem;
      position: relative;
      padding-left: 2rem;
    }

    .item:last-child {
      margin-bottom: 0;
    }

    .item::before {
      content: '◆';
      position: absolute;
      left: 0;
      top: 2px;
      color: var(--primary);
      font-size: 0.8rem;
      opacity: 0.7;
    }

    .item h4 {
      font-size: 1.2rem;
      margin-bottom: 0.3rem;
      font-weight: 600;
    }

    .item p {
      color: var(--text-light);
      font-size: 0.95rem;
    }

    /* ===== SKILLS TAGS ===== */
    .skills-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 0.8rem;
      margin: 1.5rem 0 0.5rem;
    }

    .skill-tag {
      background: linear-gradient(135deg, rgba(154, 140, 124, 0.08) 0%, rgba(154, 140, 124, 0.03) 100%);
      color: var(--primary-dark);
      padding: 0.6rem 1.4rem;
      border-radius: 40px;
      font-size: 0.85rem;
      font-weight: 400;
      border: 1px solid rgba(154, 140, 124, 0.2);
      transition: var(--transition);
      cursor: default;
    }

    .skill-tag:hover {
      background: var(--primary);
      color: white;
      border-color: var(--primary);
      transform: scale(1.05);
    }

    /* ===== TWO COLUMN LAYOUT ===== */
    .two-column {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
      margin: 2rem 0;
    }

    /* ===== PROJECTS SECTION ===== */
    .project-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
      margin: 2rem 0;
    }

    .project-card {
      background: white;
      border-radius: 16px;
      padding: 2rem;
      transition: var(--transition);
      box-shadow: var(--shadow-sm);
      border: var(--border-light);
    }

    .dark-mode .project-card {
      background: var(--light-alt);
    }

    .project-card:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow-lg);
    }

    .project-icon {
      font-size: 2.5rem;
      color: var(--accent);
      margin-bottom: 1rem;
    }

    .project-card h4 {
      margin-bottom: 1rem;
      font-size: 1.3rem;
    }

    .project-card p {
      color: var(--text-light);
      margin-bottom: 1.5rem;
    }

    .project-tags {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }

    .project-tags span {
      background: var(--light-alt);
      padding: 0.3rem 1rem;
      border-radius: 30px;
      font-size: 0.8rem;
    }

    /* ===== TESTIMONIALS ===== */
    .testimonial-slider {
      display: flex;
      gap: 2rem;
      overflow-x: auto;
      padding: 1rem 0;
      scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch;
    }

    .testimonial-card {
      min-width: 300px;
      background: white;
      padding: 2rem;
      border-radius: 16px;
      box-shadow: var(--shadow-sm);
      scroll-snap-align: start;
      transition: var(--transition);
    }

    .dark-mode .testimonial-card {
      background: var(--light-alt);
    }

    .testimonial-card:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow-md);
    }

    .quote {
      font-style: italic;
      margin-bottom: 1rem;
      position: relative;
    }

    .quote::before {
      content: '"';
      font-size: 4rem;
      color: var(--primary-light);
      position: absolute;
      left: -1rem;
      top: -1rem;
      opacity: 0.3;
    }

    .author {
      font-weight: 600;
      color: var(--primary-dark);
    }

    /* ===== CONTACT FORM ===== */
    .contact-form {
      margin: 3rem 0;
    }

    .form-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
      margin-bottom: 1rem;
    }

    .contact-form input,
    .contact-form textarea {
      width: 100%;
      padding: 1rem;
      border: var(--border-light);
      border-radius: 8px;
      font-family: 'Inter', sans-serif;
      transition: var(--transition);
      background: white;
    }

    .dark-mode .contact-form input,
    .dark-mode .contact-form textarea {
      background: var(--light-alt);
      color: white;
    }

    .contact-form input:focus,
    .contact-form textarea:focus {
      outline: none;
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(154,140,124,0.1);
    }

    .submit-btn {
      background: var(--secondary);
      color: white;
      border: none;
      padding: 1rem 2rem;
      border-radius: 40px;
      cursor: pointer;
      font-weight: 500;
      transition: var(--transition);
      margin-top: 1rem;
      width: 100%;
    }

    .submit-btn:hover {
      background: var(--accent);
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }

    /* ===== RESUME DOWNLOAD ===== */
    .resume-download {
      text-align: center;
      margin: 2rem 0;
    }

    .download-btn {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 1rem 2rem;
      background: var(--secondary);
      color: white;
      text-decoration: none;
      border-radius: 40px;
      font-weight: 500;
      transition: var(--transition);
    }

    .download-btn:hover {
      background: var(--primary-dark);
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }

    /* ===== CAREER FOCUS CARD ===== */
    .career-card {
      background: linear-gradient(135deg, var(--light-alt) 0%, white 100%);
      border: none;
      border-left: 4px solid var(--accent);
    }

    .dark-mode .career-card {
      background: linear-gradient(135deg, var(--light-alt) 0%, var(--dark) 100%);
    }

    .career-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 0.8rem;
      margin-bottom: 2rem;
    }

    .career-tag {
      background: white;
      color: var(--dark);
      padding: 0.7rem 1.5rem;
      border-radius: 40px;
      font-size: 0.9rem;
      font-weight: 500;
      box-shadow: var(--shadow-sm);
      border: var(--border-light);
      transition: var(--transition);
    }

    .dark-mode .career-tag {
      background: var(--light-alt);
      color: white;
    }

    .career-tag:hover {
      background: var(--secondary);
      color: white;
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }

    /* ===== CONTACT FOOTER ===== */
    .contact-footer {
      display: flex;
      justify-content: center;
      gap: 3rem;
      margin-top: 4rem;
      padding: 2.5rem 0;
      border-top: var(--border-light);
      background: white;
      border-radius: 60px 60px 0 0;
      flex-wrap: wrap;
    }

    .dark-mode .contact-footer {
      background: var(--light-alt);
    }

    .contact-item {
      display: flex;
      align-items: center;
      gap: 1rem;
      font-size: 1rem;
      color: var(--text);
      transition: var(--transition);
      padding: 0.5rem 1.5rem;
      border-radius: 40px;
      background: var(--light);
      cursor: pointer;
    }

    .dark-mode .contact-item {
      background: var(--light-alt);
    }

    .contact-item i {
      color: var(--accent);
      font-size: 1.2rem;
      width: 1.5rem;
    }

    .contact-item:hover {
      background: var(--primary-light);
      transform: translateY(-3px);
      box-shadow: var(--shadow-md);
    }

    /* ===== RESPONSIVE DESIGN ===== */
    @media (max-width: 900px) {
      .two-column {
        grid-template-columns: 1fr;
        gap: 1.5rem;
      }

      .nav-container {
        flex-direction: column;
        gap: 1rem;
      }

      .nav-links {
        flex-wrap: wrap;
        justify-content: center;
        gap: 1rem;
      }

      h1 {
        font-size: 2.5rem;
      }

      .motto::before,
      .motto::after {
        width: 15px;
      }

      .stats-grid {
        grid-template-columns: repeat(2, 1fr);
      }

      .hero-section h2 {
        font-size: 1.8rem;
      }
    }

    @media (max-width: 600px) {
      .contact-footer {
        flex-direction: column;
        align-items: center;
        gap: 1rem;
        padding: 2rem 1rem;
      }

      .contact-item {
        width: 100%;
        justify-content: center;
      }

      .tabs-header {
        flex-wrap: wrap;
      }

      .tab-label {
        flex: 1 1 calc(50% - 0.25rem);
      }

      .form-grid {
        grid-template-columns: 1fr;
      }

      .hero-cta {
        flex-direction: column;
      }

      .btn {
        width: 100%;
      }

      .stats-grid {
        gap: 1rem;
      }

      .stat-card {
        padding: 1rem;
      }

      .stat-number {
        font-size: 2rem;
      }
    }

    /* ===== PRINT STYLES ===== */
    @media print {
      .navbar, .theme-toggle, .hero-cta, .contact-form, .resume-download {
        display: none;
      }

      body {
        background: white;
        color: black;
      }

      .card, .tabs-container {
        box-shadow: none;
        border: 1px solid #ddd;
      }
    }
  </style>
</head>
<body>
  <!-- Loader -->
  <div class="loader-wrapper">
    <div class="loader"></div>
  </div>

  <!-- Theme Toggle -->
  <button class="theme-toggle" id="themeToggle">
    <i class="fas fa-moon"></i>
  </button>

  <!-- Navigation Bar -->
  <nav class="navbar">
    <div class="nav-container">
      <a href="#" class="logo">Atharv <span>Jadhav</span></a>
      <ul class="nav-links">
        <li><a href="#profile">Profile</a></li>
        <li><a href="#achievements">Achievements</a></li>
        <li><a href="#core-competencies">Competencies</a></li>
        <li><a href="#professional-value">Value</a></li>
        <li><a href="#career-focus">Focus</a></li>
      </ul>
    </div>
  </nav>

  <div class="container">
    <!-- Header -->
    <header class="header">
      <h1>Atharv Jadhav</h1>
      <p class="motto">Driven by clarity, committed to excellence</p>
    </header>

    <!-- Hero Section -->
    <div class="hero-section">
      <h2>Athlete & Business Professional</h2>
      <p class="hero-subtitle">Building bridges between athletic discipline and business excellence</p>
      <div class="hero-cta">
        <a href="#contact" class="btn primary">Get in Touch</a>
        <a href="#career-focus" class="btn secondary">View Opportunities</a>
      </div>
    </div>

    <!-- Stats Grid -->
    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-number">8</div>
        <div class="stat-label">Years in Sports</div>
      </div>
      <div class="stat-card">
        <div class="stat-number">20+</div>
        <div class="stat-label">Medals</div>
      </div>
      <div class="stat-card">
        <div class="stat-number">99.40</div>
        <div class="stat-label">CET Percentile</div>
      </div>
      <div class="stat-card">
        <div class="stat-number">4</div>
        <div class="stat-label">Sports</div>
      </div>
    </div>

    <!-- PROFILE SECTION -->
    <section id="profile" class="section">
      <h2 class="section-title">Profile</h2>
      <div class="card" id="profile-card">
        <p class="profile-text">A highly disciplined and goal-oriented individual with a national-level sports background having won 20+ medals and a strong academic foundation with 99.40 percentile in CET. Currently pursuing education at IIM Bangalore. With a unique blend of athletic grit and business acumen, I bring leadership, strategic thinking, and effective team management to every challenge. I thrive in diverse environments and am driven by a passion for continuous learning and growth.</p>
      </div>
    </section>

    <!-- ACHIEVEMENTS SECTION -->
    <section id="achievements" class="section">
      <h2 class="section-title">Achievements</h2>
      
      <div class="tabs-wrapper">
        <input type="radio" name="achievements" id="academic" class="tab-radio" checked>
        <input type="radio" name="achievements" id="sports" class="tab-radio">
        
        <div class="tabs-container">
          <div class="tabs-header">
            <label for="academic" class="tab-label">Academic</label>
            <label for="sports" class="tab-label">Sports</label>
          </div>
          
          <!-- Academic Content -->
          <div class="tab-content" id="academic-content">
            <div class="item">
              <h4>IIM Bangalore</h4>
              <p>Bachelor of Business Administration (2024-2027)</p>
            </div>
            <div class="item">
              <h4>SVKM's UPG College</h4>
              <p>Bachelor of Management Studies (2024-2027)</p>
            </div>
            <div class="item">
              <h4>BS Patel College</h4>
              <p>12th Science (2022-2024) · 88%</p>
            </div>
            <div class="item">
              <h4>MSM English School</h4>
              <p>10th Standard · Passed with Distinction (2022)</p>
            </div>
          </div>
          
          <!-- Sports Content -->
          <div class="tab-content" id="sports-content">
            <div class="item">
              <h4>National Cricket Player</h4>
              <p>Represented at national level tournaments</p>
            </div>
            <div class="item">
              <h4>State Football Player</h4>
              <p>Competed at state level championships</p>
            </div>
            <div class="item">
              <h4>State Level Basketball Player</h4>
              <p>Participated in regional competitions · MVP 2023</p>
            </div>
            <div class="item">
              <h4>State Level Swimmer</h4>
              <p>Achieved recognition in aquatic sports · Multiple medals</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- TWO COLUMN LAYOUT -->
    <div class="two-column">
      
      <!-- CORE COMPETENCIES -->
      <section id="core-competencies" class="section">
        <h2 class="section-title">Core Competencies</h2>
        
        <div class="tabs-wrapper">
          <input type="radio" name="competencies" id="leadership" class="tab-radio" checked>
          <input type="radio" name="competencies" id="languages" class="tab-radio">
          
          <div class="tabs-container">
            <div class="tabs-header">
              <label for="leadership" class="tab-label">Leadership</label>
              <label for="languages" class="tab-label">Languages</label>
            </div>
            
            <!-- Leadership Content -->
            <div class="tab-content" id="leadership-content">
              <div class="item">
                <h4>Strategic Leadership</h4>
                <p>Proven ability to lead teams and drive projects through strategic vision and decisive action</p>
              </div>
              <div class="item">
                <h4>Business Communication</h4>
                <p>Articulate communicator with strong presentation and negotiation skills</p>
              </div>
              <div class="item">
                <h4>Team Development</h4>
                <p>Skilled in mentoring and fostering high-performance cultures</p>
              </div>
              <div class="item">
                <h4>Analytical Problem-Solving</h4>
                <p>Structured approach to identifying and implementing solutions</p>
              </div>
            </div>
            
            <!-- Languages Content -->
            <div class="tab-content" id="languages-content">
              <div class="skills-grid">
                <span class="skill-tag">English (Full Professional)</span>
                <span class="skill-tag">Hindi (Fluent)</span>
                <span class="skill-tag">Marathi (Native)</span>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- PROFESSIONAL VALUE -->
      <section id="professional-value" class="section">
        <h2 class="section-title">Professional Value</h2>
        
        <div class="tabs-wrapper">
          <input type="radio" name="value" id="differentiators" class="tab-radio" checked>
          <input type="radio" name="value" id="interests" class="tab-radio">
          
          <div class="tabs-container">
            <div class="tabs-header">
              <label for="differentiators" class="tab-label">Differentiators</label>
              <label for="interests" class="tab-label">Interests</label>
            </div>
            
            <!-- Differentiators Content -->
            <div class="tab-content" id="differentiators-content">
              <div class="item">
                <h4>Athletic Discipline</h4>
                <p>Elite sports training with 20+ medals has instilled discipline, resilience, and a performance-driven mindset that translates directly to business leadership</p>
              </div>
              <div class="item">
                <h4>Academic Excellence</h4>
                <p>99.40 percentile in CET and strong foundation from premier institutions combining theoretical knowledge with practical business applications</p>
              </div>
            </div>
            
            <!-- Interests Content -->
            <div class="tab-content" id="interests-content">
              <div class="skills-grid">
                <span class="skill-tag">Competitive Sports</span>
                <span class="skill-tag">Financial Markets</span>
                <span class="skill-tag">Strategy & Innovation</span>
                <span class="skill-tag">Leadership Development</span>
                <span class="skill-tag">Continuous Learning</span>
                <span class="skill-tag">Behavioral Economics</span>
              </div>
            </div>
          </div>
        </div>
      </section>
    </div>

    <!-- PROJECTS SHOWCASE -->
    <section class="section">
      <h2 class="section-title">Featured Projects</h2>
      <div class="project-grid">
        <div class="project-card">
          <div class="project-icon">
            <i class="fas fa-chart-line"></i>
          </div>
          <h4>Business Strategy Project</h4>
          <p>Developed comprehensive market entry strategy for tech startup, resulting in 40% growth projection</p>
          <div class="project-tags">
            <span>Strategy</span>
            <span>Research</span>
            <span>Analysis</span>
          </div>
        </div>
        
        <div class="project-card">
          <div class="project-icon">
            <i class="fas fa-users"></i>
          </div>
          <h4>Team Leadership Initiative</h4>
          <p>Led cross-functional team of 12 members to achieve 40% efficiency improvement in operations</p>
          <div class="project-tags">
            <span>Leadership</span>
            <span>Operations</span>
            <span>Team Building</span>
          </div>
        </div>
      </div>
    </section>

    <!-- TESTIMONIALS -->
    <section class="section">
      <h2 class="section-title">Recommendations</h2>
      <div class="testimonial-slider">
        <div class="testimonial-card">
          <div class="quote">Atharv's discipline and strategic thinking set him apart. He brings the same intensity to business as he does to sports.</div>
          <div class="author">- National Cricket Coach</div>
        </div>
        <div class="testimonial-card">
          <div class="quote">Exceptional leader with natural ability to motivate teams and drive results under pressure.</div>
          <div class="author">- Professor, IIM Bangalore</div>
        </div>
        <div class="testimonial-card">
          <div class="quote">One of the most dedicated individuals I've mentored. His ability to balance academics and sports is remarkable.</div>
          <div class="author">- State Football Coach</div>
        </div>
      </div>
    </section>

    <!-- CAREER FOCUS SECTION -->
    <section id="career-focus" class="section">
      <h2 class="section-title">Career Focus</h2>
      <div class="card career-card">
        <div class="career-tags">
          <span class="career-tag">Corporate Strategy</span>
          <span class="career-tag">Operations Management</span>
          <span class="career-tag">Business Development</span>
          <span class="career-tag">Program Management</span>
          <span class="career-tag">Management Consulting</span>
        </div>
        <div class="item">
          <p class="profile-text">Seeking opportunities to apply analytical thinking and business acumen in developing strategic initiatives that drive organizational growth and operational excellence. Open to roles in strategy, operations, and business development where I can leverage my diverse background.</p>
        </div>
      </div>
    </section>

    <!-- RESUME DOWNLOAD -->
    <div class="resume-download">
      <a href="#" class="download-btn" download>
        <i class="fas fa-download"></i> Download Resume (PDF)
      </a>
    </div>

    <!-- CONTACT FORM -->
    <section id="contact" class="section contact-form">
      <h2 class="section-title">Get In Touch</h2>
      <form id="contactForm" onsubmit="event.preventDefault(); alert('Thank you for your message! This is a demo form.');">
        <div class="form-grid">
          <input type="text" placeholder="Your Name" required>
          <input type="email" placeholder="Your Email" required>
        </div>
        <input type="text" placeholder="Subject" required style="margin-bottom: 1rem;">
        <textarea placeholder="Your Message" rows="5" required></textarea>
        <button type="submit" class="submit-btn">Send Message <i class="fas fa-paper-plane"></i></button>
      </form>
    </section>

    <!-- CONTACT FOOTER -->
    <footer class="contact-footer">
      <div class="contact-item">
        <i class="fas fa-map-marker-alt"></i>
        <span>Mumbai, India</span>
      </div>
      <div class="contact-item">
        <i class="fas fa-phone-alt"></i>
        <span>+91 98340 09750</span>
      </div>
      <div class="contact-item">
        <i class="fas fa-envelope"></i>
        <span>athravjadhav721@gmail.com</span>
      </div>
      <div class="contact-item">
        <i class="fab fa-linkedin-in"></i>
        <span>LinkedIn</span>
      </div>
    </footer>
  </div>

  <!-- JavaScript -->
  <script>
    // Loader
    window.addEventListener('load', () => {
      setTimeout(() => {
        document.querySelector('.loader-wrapper').classList.add('fade-out');
      }, 500);
    });

    // Smooth scroll for navigation links
    document.querySelectorAll('.nav-links a').forEach(link => {
      link.addEventListener('click', function(e) {
        e.preventDefault();
        const targetId = this.getAttribute('href');
        const targetSection = document.querySelector(targetId);
        
        if (targetSection) {
          targetSection.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });

    // Dark mode toggle
    const themeToggle = document.getElementById('themeToggle');
    const icon = themeToggle.querySelector('i');
    
    themeToggle.addEventListener('click', () => {
      document.body.classList.toggle('dark-mode');
      if (document.body.classList.contains('dark-mode')) {
        icon.classList.remove('fa-moon');
        icon.classList.add('fa-sun');
      } else {
        icon.classList.remove('fa-sun');
        icon.classList.add('fa-moon');
      }
    });

    // Contact form handling
    const contactForm = document.getElementById('contactForm');
    if (contactForm) {
      contactForm.addEventListener('submit', (e) => {
        e.preventDefault();
        alert('Thank you for your message! This is a demo form. In a real implementation, this would send an email.');
        contactForm.reset();
      });
    }

    // Download button
    const downloadBtn = document.querySelector('.download-btn');
    if (downloadBtn) {
      downloadBtn.addEventListener('click', (e) => {
        e.preventDefault();
        alert('This is a demo. In a real implementation, this would download your resume PDF.');
      });
    }

    // Contact items click
    document.querySelectorAll('.contact-item').forEach(item => {
      item.addEventListener('click', () => {
        const text = item.querySelector('span').textContent;
        if (text.includes('@')) {
          navigator.clipboard.writeText(text);
          alert('Email copied to clipboard!');
        } else if (text.includes('98340')) {
          navigator.clipboard.writeText('+919834009750');
          alert('Phone number copied to clipboard!');
        } else if (text.includes('LinkedIn')) {
          window.open('https://linkedin.com/in/atharvjadhav', '_blank');
        }
      });
    });
  </script>
</body>
</html>
